using System;
using System.Collections.Generic;
using System.Text;
using Restaurant.DataAccess.Repositories;
using Restaurant.DataAccess.Model;
using Xunit;

namespace Restaurant.Tests
{
    public class TestStoreRepositoryA
    {
       /* [Fact]
        public void QueryCustomersFindById()
        {
            var context = new DbRestaurantContext();
            var custRepo = new CustomerRepository(context);

            var customerId = 1;
            var includeName = "Orders";
            var customer = custRepo.Get(customerId, includeName);

            Console.WriteLine("ID " + customer.CustomerId + " Name " + customer.FullName);
            Console.WriteLine("Order: " + customer.Orders.ToString());
        }*/
    }
}
